# 设计一致性规则

## 以体验目标判断完成

用户要求的玩法、画面、操作感受及明确认可的参考效果，是完成判断的依据。技术选型、功能接入、参数修改或测试通过不能单独证明目标达成。通过实际运行、操作或可比条件下的画面对照检查效果；未达到时继续调整，遇到限制则说明差距与取舍，不自行降低目标后宣布完成。参考只采用用户认可的部分，不扩展为整套风格或设计授权。仅修改文档时按文档目标检查。交付结论回应原体验要求，说明实际观察或对照依据及未达到部分；不能只列技术修改与测试通过，无需另建报告。

## 找到并保持设计依据

- 动手前明确依据：首次接手、缺少背景或涉及整体方向时读 [PROJECT](docs/PROJECT.md)。修改设计或玩家可感知行为前，从 PROJECT 的系统入口定位并读取相关 System；入口缺失时搜索现有资料，避免漏读或重复新建。只有相关内容已读且仍有效，或当前对话已完整提供依据时才复用，不凭文件名或旧印象判断。
- PROJECT 保存项目方向与核心要求；System 保存纯设计细节，分为“已确认设计”和“AI 当前方案”，结构见 [系统说明](docs/systems/README.md)。技术实现与任务过程不混入设计正文。
- 只有用户明确确认的内容进入“已确认设计”，简要保留来源与认可范围；局部认可、沉默、已实现或测试通过不代表整体确认。用户已明确的要求即使尚未落盘也须保留。
- AI 当前方案可在已确认边界与授权任务范围内自主调整；涉及体验的重要变化说明原因与结果，触碰确认边界先对齐。不为匹配现有代码修改用户目标。
- 设计变化回到正文：本轮新增明确要求或改变当前玩法时，交付前更新对应 PROJECT 或 System；新建 System 时补入 PROJECT 的系统入口。只改技术、设计未变则无需更新。用户要求仅讨论或不落盘时遵从。保留未实现或未达目标的差距；要求冲突先核实来源，无法判定的设计取舍再向用户确认。
- 规划、技术记录与验证方法由 AI 按任务决定，Plan 完全可选。已有 Plan 或历史结果不覆盖当前设计依据；不因本框架强制建档、划阶段或提交代码。

## 交付收尾与证据一致性

- 文档收尾从项目入口核对：受影响的 System、运行说明与实际行为一致，区分并存模式的适用范围。形成持久验收记录时接入已有索引或工程入口；旧计划的“当前状态”和“下一步”标明历史日期，不覆盖后续授权。仅技术修改不强制更新设计。
- 交付区分本次通过、失败未关闭、未执行、沿用历史和已过期结果。失败后重跑保留两次记录；重跑通过不自动证明偶发问题根因已解决。体验审核与机器检查分别说明，文件齐全不代替体验达标。
- 作为交付依据的验证应能识别受测源码、相关资产、配置与环境；未提交工作区不能只引用 HEAD，按复核需要保存相关文件摘要及必要差异或快照。摘要只能识别状态，不能代替可复现的文件。验证使用新目录，不覆盖原件；相关输入随后改变时，旧结果不能直接作为当前通过依据。历史缺口如实保留。
- 交付可玩入口前，核实目标服务或程序启动、页面/场景加载及主入口操作；修改启动方式时按影响检查首次启动和重复启动，未覆盖的条件明确说明。不得自动停止其他项目服务；端口或数据位置变化须说明对存档的影响。
- 自动检查和 hook 按项目需要采用，不是框架接入前提。先用可直接运行的脚本验证确定问题，再考虑提醒或阻断；普通讨论、诚实的失败报告和用户中断不应被强制续跑。工具异常不得当作通过，宿主加载未经实测不得声称 hook 已生效。具体命令与配置留在工程说明，不强制新增 Plan、报告模板、全量测试或默认提交。


首次接入或升级读 [接入指南](docs/ADOPTION.md)。遵守实际权限、用户范围与停止条件，保护已有工作；提交、合并、推送等沿用项目和用户授权。本框架不额外授予这些权限。
