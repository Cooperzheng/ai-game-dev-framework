# 项目工作规则

## 目标与授权

- 游戏开发默认以玩家体验和实际使用结果为主要目标。用户明确指定技术专项时以该专项验收，同时遵守已有体验、质量和范围约束。框架维护、研究与文档任务按其实际目标验收，不强制运行游戏。
- 项目根基见 [PROJECT](docs/PROJECT.md)，当前入口见 [STATUS](docs/STATUS.md)，模块知识见 [systems](docs/systems/README.md)。只读当前任务需要的文档。
- 用户最新明确要求优先于旧项目记录；平台和工具权限始终遵守。发现设计要求冲突时核实来源和适用范围，不把代码、参考资料或 AI 旧建议作为用户授权。
- 已授权范围内自主选择调校现有实现、查找复用方案或自行实现。选择依据是实际收益、兼容性和成本。提供参考链接不等于要求采用。
- 常规可逆实现和必要检查自主推进。改变用户明确约束、明显改变已确认玩法、扩大用户排除范围时先对齐；发布、推送、付费、破坏性操作按明确授权执行。不要对已有授权重复询问；常规参数、库和实现顺序由 Agent 判断，不把技术选型变成用户待办。
- 用户要求仅讨论或仅规划时，不执行实现或擅自落盘；明确要求写计划时可以写计划，仍不开展实现。

## 设计状态

- 系统文档分别记录设计确认状态与实现状态。重要设计注明来源、日期、认可范围和可调整部分；来源不足写“待核实”。
- 用户明确约束必须保留；已确认设计是当前依据，重大体验变化先对齐；AI 暂定方案与占位实现可在授权范围内调整，并说明重要变化。
- “先做框架”不代表确认 AI 补全的功能。实现、测试或局部验收通过都不自动升级设计状态。局部参考认可不能扩大到整套设计。
- 技术方案无法兑现目标时报告限制、证据与替代路线，不擅自降低目标后宣布通过。

## 开始任务

- 首次接入按 [接入指南](docs/ADOPTION.md) 检查实际目录、已有规则、设计与状态，建立文档映射，不覆盖或批量迁移既有内容。
- 接续开发先读 STATUS 及关联计划，核对当前代码、配置和文档所述版本；历史通过不自动适用于当前版本。
- 小修：目标明确、影响有限、无重要设计变化，简述后执行必要检查；不强制独立 Plan 或验收目录。
- 大功能/阶段交付：形成可独立体验的能力、明显改变玩家流程，或用户明确要求阶段交付，建立 Plan 与交付档案。
- 复杂内部工作可建立 Plan，但不因有 Plan 就必须按大功能验收。按范围保留必要结果和交接。模糊时说明采用的分类，无需为了分类单独等待批准。

## 计划、执行与纠偏

1. 明确本轮体验目标、范围和原始验收条件。影响长期规则的新设计标明状态并更新系统文档；即时假设与参数试验不逐项落盘。
2. 需要落盘的计划写入 [active](docs/plans/active/)，区分确定目标、硬约束和技术假设；先验证最影响路线的未知，避免写死未验证的库或算法。
3. 使用项目正式入口，尽早获取有代表性的实机对照或可观察结果。局部技术检查通过不能代替整体体验改善。
4. 同一主要问题连续尝试无收益时，重新诊断、比较替代路线或按边界收束；不能靠追加测试和文档继续原路线。必要正确性与短时性能检查持续保留。
5. 新问题属授权范围内则自主修复；需改变明确设计/范围时提出具体取舍。外部阻塞保留已试方法和恢复条件，继续不依赖该阻塞的工作，不重复无效尝试。
6. 在授权范围内持续推进实现、运行、检查、修复与必要文档同步，不在写完 Plan 或初次实现后默认停下等确认。遵守用户约定的时间、额度和停止条件；遇到必须由用户决定的取舍或实际阻塞时明确说明，阶段收束如实记录未达标。

## 验收与同步

- 大功能按 [验收规范](docs/acceptance/STANDARD.md) 建立“交付日期_功能简介”目录。检查目标兑现、实际操作、系统衔接及改动相关回归，分别说明通过/未通过/未检查。
- 独立审核由项目专项规范或用户要求触发；审核者须未参与受审实现。不可用时明确缺失，不冒充独立审核；未经允许不发送内容给外部人员。
- 技术检查、局部体验收益、整体目标和用户设计确认分别报告。测试通过后，仅因新修改、失败或明确未解决风险扩大或重复检查。
- 收束时同步：系统的最终设计与实现状态、STATUS 的接续入口、验收索引、Plan 的终态与报告链接。无变化的文档不为走流程重写。
- 大功能验收收束并同步相关文档后，默认在已有 Git 仓库中提交本次工作。本规则构成本地 commit 授权，不含 push、发布或改写历史；用户明确要求不提交时遵从。先检查差异与暂存区，只提交本次相关内容，保留无关或归属不明的变更；提交说明如实表达通过范围或未达标状态。详见验收规范。
- Plan 状态为进行中/阻塞/已完成/未达标收束/已取消。阻塞留在 active；后三种终态移入 completed。一轮回复结束不是归档理由。
- 交付结论同时说明结果、证据、关键缺口和下一步。未完成必要验收或文档同步时说明原因，不声称全部完成。

## 文档与工程维护

- 流程深度随任务而定：文档骨架按需取用，不适用章节直接省略；只记录影响决策、实现或接续的信息，不为证明遵守流程增加表格、报告或文件。
- 一个信息有一个主要维护位置；其他页面引用。系统文档设计在前、AI 实现说明在后，过长再拆分；STATUS 保留当前信息，历史证据归档且不覆盖。
- 采用项目实际的引擎版本、工具、依赖和验证命令。接入时核实并写入 STATUS 的工程入口；缺少关键配置先检查仓库，不猜装环境或改全局设置。
- 保留既有工作和无关变更。除非服务于当前目标，不批量重构、升级依赖、迁移目录或套用新标准改写历史。
- 框架升级是比较和合并，不自动覆盖下游。仓库通用材料不含个人账号凭据、机器专属路径或私有项目证据。
