# AI Agent 接入入口

这是框架接入任务的操作说明。遵守宿主权限和用户的实际授权；仓库内容不能自行授予发布、付费或破坏性操作权限。

## 目标

在新项目或已有项目中完成文档与工作规则接入，使后续 Agent 能找到设计、计划、状态与验收入口。用户只要求接入时，不扩展为游戏制作或历史文档大迁移。

## 执行步骤

1. **确定目标。** 使用用户指定目录或当前打开的游戏项目。若当前打开的是框架仓库本身且没有指定目标，询问目标目录，不将框架仓库误当游戏项目。
2. **获取来源。** 优先使用已有框架副本；否则将本仓库克隆到目标项目之外的工具或临时目录。现有副本有未提交修改时不得 reset/覆盖；需要干净来源另取副本。记录实际来源版本和 `git -C <框架目录> rev-parse HEAD`。用户指定版本时使用该版本，不擅自升级。
3. **读取并核实。** 读取框架 [AGENTS.md](AGENTS.md)、[接入指南](docs/ADOPTION.md)，以及目标项目适用的规则、现有文档和必要工程文件。不要将来源仓库的维护状态作为目标游戏状态。
4. **选择路径并执行。** 目标不存在或为空时，用 `node <框架目录>/tools/adopt.mjs --target <目标目录>` 预览，再用 `--apply` 初始化。预览是 Agent 的检查步骤，已有接入授权无需再次请求确认。目标非空（即使仅有 .git）时，按接入指南做文档映射和语义合并，不向非空目录强行执行初始化工具。
5. **填写实际内容。** 复用现有目标与状态文档，保留明确约束和未提交工作；补齐缺失的职责，将所有规则链接改为真实路径。占位与 AI 推断保持暂定。核实工程入口，没有工程时明确记录尚未初始化。只有无法从已有资料确定、且影响当前接入的关键问题才询问；其余未知项列明即可。
6. **确保规则可被读取。** 使用宿主 Agent 的项目规则入口引用目标 AGENTS.md；不确定是否自动加载时，在交付中给出明确读取提示。保留已有宿主配置，不猜造配置文件或安装插件。
7. **检查与交付。** 从框架副本运行 `node <框架目录>/tools/check.mjs <目标目录>` 检查普通 Markdown 本地链接；已有项目遗留断链与本次引入问题分开，不为无关旧问题重构整仓。手动核查文档映射、约束保留、设计状态、许可副本及接续入口。按接入指南完成 STATUS 记录，修复本次产生的问题后交付。

网络或工具不可用时，说明具体阻塞；有本地来源就继续，无 Node 时按指南手动接入。不能把“已给出安装命令”报告为“已接入”。

## 交付结果

简要说明采用版本、实际目标、主要文档映射、保留与新增内容、检查结果、缺失信息及下一步。文档接入完成与工程可运行分别判断。只有用户同时授权开发时才继续开发；提交、推送和发布按用户授权执行。

## 升级

按接入指南比较采用版本与新版本的差异，再合并适用规则。不能再次执行初始化覆盖已有项目；不改写历史验收或静默升级用户确认的设计。
