# 文档职责精简

- 类型与状态：内部工作；已完成。
- 开发方式：沿用 main 工作区；前一轮相关修改尚未提交，保留现有成果，不切换或重置。基准为 `7a9cad3`，本轮不提交、合并或推送。
- 依据：用户于 2026-09-18 确认将日常维护收敛到 AGENTS、PROJECT、System、Plan。

## 问题与范围

合并项目目标与状态，Plan 同时承载执行与验收结果，移除重复导航和验收维护位置。更新初始化生成内容、接入和升级说明、中英文入口与示例；保留原有设计确认、版本证据和授权边界。仅修改本框架，不迁移其他游戏。

## 尝试与重要取舍

- AGENTS 直接规定每轮阅读 PROJECT、相关 System 与 Plan 的条件。
- 本仓库 PROJECT 描述框架自身；生成器独立生成目标项目占位，防止把框架状态复制进游戏。
- 新工作验收归入 Plan；历史独立报告保留原事实，放对应任务目录并链接，无关联 Plan 时不补造。
- 证据与任务一起归档，外部大文件保留固定位置。参考资料按需使用，不预建空目录。
- 第一轮精简检查为 17 份 Markdown 无缺失、5 项初始化测试通过；该结果不代替本轮检查。

## 结果与下一步

2026-09-18，Windows / Node.js v24.11.1：本轮内部规则与初始化改动通过以下检查，未提交、未推送。

- 自查：阅读路由直接可达；小修、探索转正式、独立报告例外、历史报告保留、无 Git 验收与证据归档的职责一致。修正“多步内部工作必须建 Plan”的歧义。这是编写者自查。
- 本地 Markdown 检查：14 份、0 个缺失目标；已核对现有锚点与旧入口引用。历史词语与测试中的旧路径用于历史说明和反向检查，不是当前路由。
- 初始化测试：5 项通过，覆盖只预览不写入、中文空格路径、新路由、原文件保护、参数和 junction 拒绝。实际生成 5 份 Markdown，无 STATUS、额外文档导航、acceptance 或 references。
- 差异检查通过。复验命令见 PROJECT；测试使用临时项目，无须启动游戏。真实游戏迭代、已有游戏迁移和各 Agent 宿主自动加载尚未实测。

受测工具身份（SHA-256）：adopt.mjs = 52AEFCB8408686FD964D7FCBAA28078E5A65428DB6819D5B540902AF7EA08610；adopt.test.mjs = 5333CF3F7AE8A2B1383AE8987A0D2E14386EC8406D74C8AAAF4636525E990958。基准 commit 不代表全部工作区；该记录用于本轮工具检查。

下一步用真实玩法探索到正式接入的迭代检验维护成本，不自动迁移其他项目。
